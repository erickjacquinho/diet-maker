{
  "UserPromptSubmit": [
    {
      "matcher": "*",
      "hooks": [
        {
          "type": "prompt",
          "prompt": "Evaluate if the user prompt involves component creation, refactoring, or reorganization under src/components/. Remind that all component decisions MUST strictly comply with the 5-level Atomic Design hierarchy defined in c:\\Programmer\\diet-maker\\.agents\\rules\\atomic-design.md."
        }
      ]
    }
  ],
  "PreToolUse": [
    {
      "matcher": "Write|Edit",
      "hooks": [
        {
          "type": "prompt",
          "prompt": "Validate if the current file creation, edit, or relocation targets any file under src/components/. If targeting src/components/, review c:\\Programmer\\diet-maker\\.agents\\rules\\atomic-design.md to verify that: 1. Atoms (atoms/) are indivisible primitive elements with zero imports of molecules/organisms. 2. Molécules (molecules/) are simple combinations of 2+ atoms with a single functional purpose. 3. Organisms (organisms/) are complex layout/section components. 4. Templates (templates/) are page layout skeletons. If the component placement or structure violates atomic-design.md rules, return permissionDecision 'deny' or systemMessage with instructions to correct the location."
        }
      ]
    }
  ]
}
