/**
 * @deprecated Compatibility facade for clinical modules that still belong to
 * later SDDs. The patient/account application flow must use
 * `@/lib/application/browser-composition` and never this facade.
 */
export * from './legacyClinicalStore';
